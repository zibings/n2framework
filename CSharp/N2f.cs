using System;

namespace N2f
{
    public class N2f
    {
			//protected static readonly Lazy<N2f> Lz = new Lazy<N2f>(() => new N2f());
			protected static readonly N2f _Instance = new N2f();

			static N2f() { }

			private N2f()
			{
				// stuff here
			}
    }
}
